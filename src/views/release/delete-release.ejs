<div id="deleteRelease" class="modal">
    <form action="" method="post" class="deleteForm">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="modal-content center-align">
            <h4>Suppression de la Release</h4>
            <p>Souhaitez-vous réellement supprimer cette release ?</p>
        </div>
        <div class="modal-footer">
            <a class="btn-flat green-text modal-close">Annuler</a>
            <button type="submit" class="btn-flat red-text">Supprimer</button>
        </div>
    </form>
</div>

<script>
  $(document).ready(function () {
    $('.modal').modal();

    const form = document.querySelector(".deleteForm");
    const buttons = document.querySelectorAll(".deleteButton");
    console.log("form:" + form);
    buttons.forEach(button => button.addEventListener("click", () => {
      const releaseId = button.getAttribute("_id");
      form.setAttribute("action", "/projects/<%=project.id%>/releases/" + releaseId + "?_method=DELETE");
    }));
  });
</script>
